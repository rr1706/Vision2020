#include "ntcore.h"
#include "networktables/NetworkTable.h"
#include "networktables/NetworkTableInstance.h"
#include "Network.hpp"
#include "string"

using std::shared_ptr;
using namespace std;

void startTable(){
	NetworkTable::SetClientMode();
	nt::NetworkTableInstance::GetDefault().StartClient();
	//ip = 10.te.am.2
}

//this will add to the smart dashboard which can be pulled from the the driver station
void sendMessage(string entryName, double input){
	shared_ptr<NetworkTable> myTable = NetworkTable::GetTable("VisionTable");
    	entry = myTable->GetEntry(entryName);
	entry.SetDouble(input);
	//myTable->NetworkTable::PutString( entry, to_string(input) );
}
